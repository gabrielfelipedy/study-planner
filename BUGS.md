# Remaining Bugs

## dnd-kit hydration mismatch

**Error:** `aria-describedby` attribute differs between server and client

**Status:** Fixed — deferred `{...attributes}` spread until after mount in `TopicCard`
